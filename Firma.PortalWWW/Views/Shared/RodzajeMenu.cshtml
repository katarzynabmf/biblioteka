@model IEnumerable<Firma.Data.Data.CMS.Strona>
<div class="nav-wrapper">
    <a id="logo-container" href="#" class="brand-logo logo">
        <i class="material-icons">spa</i>Biblioteka
    </a>
    <ul class="right hide-on-med-and-down margin__right">
        <li class="border border-dark black-text">@Html.ActionLink("Strona główna", "Index", "Home")</li>
        @*@foreach (var item in Model)
        {
            <li>
                @Html.ActionLink(item.Tytul, "Index", "Home", new { id = item.IdStrony }, null)
            </li>
        }*@
        <li>
            @Html.ActionLink("Koszyk", "Index", "Sklep")
        </li>
        <li>
            <a href="@Url.Action("Index", "Koszyk")">
                <i class="material-icons">shopping_basket</i>
            </a>
        </li>

    </ul>

    <ul id="nav-mobile" class="sidenav">
        <li class="border border-dark black-text">@Html.ActionLink("Strona główna", "Index", "Home")</li>
        @*@foreach (var item in Model)
        {
            <li>
                @Html.ActionLink(item.Tytul, "Index", "Home", new { id = item.IdStrony }, null)
            </li>
        }*@
        <li>
            @Html.ActionLink("Koszyk", "Index", "Sklep")
        </li>
        <li>
            <a href="@Url.Action("Index", "Koszyk")">
                <i class="material-icons">shopping_basket</i>
            </a>
        </li>
    </ul>
    <a href="#" data-target="nav-mobile" class="sidenav-trigger"><i class="material-icons">menu</i></a>
</div>
